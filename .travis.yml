sudo: false
language: go
go:
  - master
  - 1.9
  - 1.8
  - 1.7
install: true # don't try to install missing deps. everything is in vendor/
before_script:
  - cd vendor/github.com/golang/lint/golint
  - go install
  - cd -
script:
  - ./test.sh
notifications:
  email:
    recipients:
      - kyle@cloudflare.com
      - zi@cloudflare.com
    on_success: change
    on_failure: change
